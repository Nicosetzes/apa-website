<%- include('./../partials/header')-%>

    <main>

        <% if(idProvided){ %>
            <div>El ID solicitado no corresponde al de ningún torneo de la base de datos!</div>
            <div>¡Intente de nuevo!</div>
            <% } else{ %>
                <div>No existen torneos en la base de datos.</div>
                <div>Antes de cargar un partido, debe <a href="/create-tournament">generar un torneo!</a></div>
                <% } %>

                    <a href="./">Volver</a>
    </main>
    <%- include('./../partials/footer')-%>